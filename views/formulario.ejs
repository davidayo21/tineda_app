<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= titulo %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f8fafc; display: flex; align-items: center; min-height: 100vh; }
        .form-box { background: white; border-radius: 16px; padding: 40px; border: 1px solid #e2e8f0; width: 100%; max-width: 550px; margin: auto; }
        label { font-weight: 700; font-size: 0.8rem; color: #475569; margin-bottom: 8px; text-transform: uppercase; }
        .form-control, .form-select { border-radius: 8px; padding: 12px; border: 1.5px solid #e2e8f0; margin-bottom: 20px; }
        .btn-confirm { background: #0f172a; color: white; border: none; width: 100%; padding: 14px; border-radius: 10px; font-weight: bold; }
        .btn-confirm:hover { background: #1e293b; }
    </style>
</head>
<body>
    <div class="container">
        <div class="form-box shadow-sm">
            <h2 class="text-center fw-bold mb-4" style="color:#0f172a"><%= titulo %></h2>
            <form action="<%= accion %>" method="POST">
                <label>Nombre del Producto</label>
                <input type="text" name="nombre" class="form-control" value="<%= producto.nombre || '' %>" required>
                
                <div class="row">
                    <div class="col-6">
                        <label>Precio ($)</label>
                        <input type="number" name="precio" class="form-control" value="<%= producto.precio || '' %>" required>
                    </div>
                    <div class="col-6">
                        <label>Categoría</label>
                        <select name="categoria" class="form-select">
                            <option value="computadores" <%= producto.categoria === 'computadores' ? 'selected' : '' %>>Computadores</option>
                            <option value="perifericos" <%= producto.categoria === 'perifericos' ? 'selected' : '' %>>Periféricos</option>
                        </select>
                    </div>
                </div>

                <label>Link de Imagen</label>
                <input type="text" name="imagen" class="form-control" value="<%= producto.imagen || '' %>">

                <label>Descripción</label>
                <textarea name="descripcion" class="form-control" rows="3"><%= producto.descripcion %></textarea>

                <button type="submit" class="btn-confirm">CONFIRMAR CAMBIOS</button>
                <div class="text-center mt-3">
                    <a href="/admin" class="text-decoration-none text-muted small fw-bold">← VOLVER AL PANEL</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>