<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Inventario | Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8fafc; }
        .admin-header { background: #0f172a; color: white; padding: 30px 0; margin-bottom: 40px; }
        .card-table { background: white; border-radius: 12px; border: none; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .table thead { background: #f1f5f9; }
        .table th { border: none; padding: 15px; color: #64748b; font-size: 0.85rem; text-transform: uppercase; }
        .table td { padding: 15px; vertical-align: middle; border-bottom: 1px solid #f1f5f9; }
        .btn-edit { background: #ecfdf5; color: #059669; border: none; border-radius: 6px; padding: 5px 12px; text-decoration: none; font-weight: 600; }
        .btn-delete { background: #fef2f2; color: #dc2626; border: none; border-radius: 6px; padding: 5px 12px; font-weight: 600; }
    </style>
</head>
<body>
    <div class="admin-header shadow">
        <div class="container d-flex justify-content-between align-items-center">
            <h2 class="m-0 fw-bold">Gestión de Inventario</h2>
            <div class="d-flex gap-2">
                <a href="/" class="btn btn-outline-light btn-sm px-3">Ver Tienda</a>
                <a href="/admin/nuevo" class="btn btn-primary btn-sm px-3 fw-bold">+ Nuevo</a>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="card card-table overflow-hidden">
            <table class="table mb-0">
                <thead>
                    <tr>
                        <th>Producto</th>
                        <th>Categoría</th>
                        <th>Precio</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% productos.forEach(item => { %>
                        <tr>
                            <td class="fw-bold"><%= item.nombre %></td>
                            <td><span class="badge bg-light text-dark border"><%= item.categoria %></span></td>
                            <td class="fw-bold text-primary">$<%= item.precio %></td>
                            <td class="text-end">
                                <a href="/admin/editar/<%= item._id %>" class="btn-edit me-2">Editar</a>
                                <form action="/admin/eliminar/<%= item._id %>" method="POST" class="d-inline">
                                    <button class="btn-delete" onclick="return confirm('¿Borrar?')">Borrar</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>