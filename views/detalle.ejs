<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Detalles</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: white; font-family: sans-serif; }
        .detail-nav { background: #0f172a; padding: 15px 0; margin-bottom: 50px; }
        .img-box { background: #f8fafc; border-radius: 20px; padding: 40px; text-align: center; border: 1px solid #f1f5f9; }
        .img-box img { max-width: 100%; height: auto; max-height: 400px; }
        .price-big { font-size: 3rem; font-weight: 800; color: #0f172a; }
        .desc-text { color: #64748b; line-height: 1.8; font-size: 1.1rem; }
    </style>
</head>
<body>
    <div class="detail-nav">
        <div class="container"><a href="/" class="text-white text-decoration-none fw-bold">← Volver a la tienda</a></div>
    </div>

    <div class="container">
        <div class="row g-5 align-items-center">
            <div class="col-md-6">
                <div class="img-box shadow-sm">
                    <img src="<%= producto.imagen %>" alt="">
                </div>
            </div>
            <div class="col-md-6">
                <span class="badge bg-primary mb-2 rounded-pill px-3"><%= producto.categoria %></span>
                <h1 class="display-4 fw-bold mb-3"><%= producto.nombre %></h1>
                <div class="price-big mb-4">$<%= producto.precio %></div>
                <div class="p-4 bg-light rounded-3 mb-4">
                    <h6 class="fw-bold text-uppercase small text-secondary">Especificaciones Técnicas:</h6>
                    <p class="desc-text mb-0"><%= producto.descripcion %></p>
                </div>
                <button class="btn btn-dark btn-lg w-100 rounded-pill py-3 fw-bold">COMPRAR AHORA</button>
            </div>
        </div>
    </div>
</body>
</html>